[gd_resource type="StandardMaterial3D" load_steps=3 format=3 uid="uid://c0ih5jtcypm4o"]

[sub_resource type="Shader" id="Shader_rwbu0"]
code = "shader_type spatial;

render_mode cull_front, unshaded, depth_draw_never;

global uniform vec4 outline_color;
global uniform float outline_width;

void vertex() {
	vec4 clip_position = 
			PROJECTION_MATRIX *
			(MODELVIEW_MATRIX * vec4(VERTEX, 1.0));
	vec3 clip_normal =
			mat3(PROJECTION_MATRIX) *
			(mat3(MODELVIEW_MATRIX) * NORMAL);
	clip_position.xy +=
			normalize(clip_normal.xy) /
			VIEWPORT_SIZE *
			clip_position.w *
			outline_width;
	POSITION = clip_position;
}

void fragment() {
	ALBEDO = outline_color.rgb;
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_od2pk"]
render_priority = 0
shader = SubResource("Shader_rwbu0")

[resource]
next_pass = SubResource("ShaderMaterial_od2pk")
albedo_color = Color(0.223529, 0.258824, 0.25098, 1)
metallic = 1.0
metallic_specular = 0.49
rim_enabled = true
rim = 0.27
rim_tint = 0.87
clearcoat_enabled = true
clearcoat_roughness = 0.78
anisotropy_enabled = true
anisotropy = -0.01
